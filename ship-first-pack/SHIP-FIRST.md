# Ship-first (collapsed)

See **START.md** — 3 steps tonight.

Download: open `index.html` or unzip `ignition-kit.zip` into your repo root.

Dispatch: `APPENDED > 0` OR `FIREABLE > 0`. Track A = quiet CI. Track B = one slice per promotion with pre-wired pipeline row.
